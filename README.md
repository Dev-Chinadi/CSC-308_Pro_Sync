# CSC-308 **Process Synchronization Practicals.**

How to Compile and Run.
All programs require the pthread library.
Use gcc to compile:

# Example for Practical 1:
gcc -pthread -o mutex mutex_practical1.c

./mutex
